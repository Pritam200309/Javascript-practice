/*
    arithmatic expression is combination of...
    operands (values, variables, etc.)
    operators (+ - / %)
    that can be evaluted to a value
    ex. y = x + 5;
 */

let students = 20;

// students = students + 1;
// students = students - 1;
// students = students * 2;
// students = students / 2;
// let extraStudents = students % 3;

// students += 1;
// students -= 1;
// students *= 2;
// students /= 2;

/* 
    operator precedence
    1. parenthesis ()
    2. exponents 
    3. multiplication & divison
    4. addition & substraction
*/

let result = 1 + 2 * (3 + 4);

console.log(result);